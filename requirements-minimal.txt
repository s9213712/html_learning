# Minimal server startup/runtime dependencies.
#
# This layer is intentionally small: it covers packages imported during app
# startup plus the bounded WSGI runner used for deployment/QA. It should not
# pull local AI model stacks, browser automation, or heavyweight feature tools.

flask>=3.0
cryptography>=42
flask-talisman>=1.1
argon2-cffi>=23.1
gunicorn>=22.0
numpy

# Imported by route bundles during server startup.
websocket-client>=1.8

# Game route startup dependencies are split out for ownership clarity, but are
# still required by the minimal startup layer until game route imports become
# fully lazy/optional.
-r requirements-games.txt

# Cloud Drive / upload security: image metadata normalization and inspection.
Pillow>=10.0
